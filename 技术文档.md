# NetworkSpeedBar — macOS 菜单栏网速监控工具

## 项目概述

一个轻量级的 macOS 菜单栏应用，实时显示当前网络的上传和下载速率，无需打开任何窗口即可随时查看网速。

---

## 技术选型

| 维度 | 选择 | 理由 |
|------|------|------|
| 语言 | Swift | 原生性能，系统 API 支持最完整 |
| UI 框架 | SwiftUI + AppKit | SwiftUI 构建菜单，AppKit 控制 NSStatusItem |
| 网速数据 | SystemConfiguration / `getifaddrs` | 直接读取系统网络接口统计，无需权限申请 |
| 定时刷新 | Timer (1 秒间隔) | 平衡实时性与 CPU 占用 |
| 分发方式 | 直接运行 / Homebrew Cask | 无需上架 App Store，不受沙盒限制 |

---

## 系统架构

```
NetworkSpeedBar
├── App Entry
│   └── NetworkSpeedBarApp.swift       # @main，配置 LSUIElement 隐藏 Dock 图标
├── Menu Bar
│   └── StatusBarController.swift      # 管理 NSStatusItem，维护菜单
├── Core
│   └── NetworkMonitor.swift           # 读取网络接口字节数，计算速率
├── Model
│   └── SpeedSnapshot.swift            # 上一次采样的字节数快照
└── Resources
    └── Info.plist                     # LSUIElement = YES（纯菜单栏 App）
```

---

## 核心模块设计

### 1. NetworkMonitor（数据层）

**原理：** 通过 `getifaddrs` 系统调用遍历所有网络接口（en0/en1/utun 等），累加 `ifi_ibytes`（接收字节）和 `ifi_obytes`（发送字节），与上一秒的快照做差值，得到当前速率。

```swift
// 伪代码示意
func currentBytes() -> (rx: UInt64, tx: UInt64) {
    var ifaddr: UnsafeMutablePointer<ifaddrs>?
    getifaddrs(&ifaddr)
    // 遍历接口，排除 lo0（loopback），累加 if_data.ifi_ibytes / ifi_obytes
    freeifaddrs(ifaddr)
    return (totalRx, totalTx)
}

// 每秒调用一次，差值即速率
let speed = currentBytes() - lastSnapshot
```

**过滤规则：**
- 排除 `lo0`（本地回环）
- 可选：只统计物理接口 `en*`，或包含 VPN 虚拟接口 `utun*`

### 2. StatusBarController（UI 层）

- 使用 `NSStatusBar.system.statusItem(withLength: NSStatusItem.variableLength)` 创建状态栏项
- `statusItem.button?.title` 直接设置文字，例如：`↑ 1.2 MB/s  ↓ 3.4 MB/s`
- 右键点击显示下拉菜单，包含：
  - 当前接口名称
  - 累计流量统计
  - 偏好设置入口
  - 退出按钮

### 3. 速率格式化

```
< 1 KB/s    → 显示 "xxx B/s"
< 1 MB/s    → 显示 "xxx KB/s"
>= 1 MB/s   → 显示 "x.xx MB/s"
>= 1 GB/s   → 显示 "x.xx GB/s"
```

---

## 菜单栏显示效果

```
┌─────────────────────────────────┐
│  ↑ 256 KB/s   ↓ 1.8 MB/s  ...  │  ← 状态栏常驻文字
└─────────────────────────────────┘

点击后展开菜单：
┌─────────────────────────────┐
│  网络接口: en0 (Wi-Fi)       │
│  ─────────────────────────  │
│  本次上传:   12.3 MB         │
│  本次下载:   87.6 MB         │
│  ─────────────────────────  │
│  偏好设置...                 │
│  开机自启动  ✓               │
│  ─────────────────────────  │
│  退出                        │
└─────────────────────────────┘
```

---

## 偏好设置（可选功能）

| 设置项 | 默认值 | 说明 |
|--------|--------|------|
| 刷新间隔 | 1 秒 | 可选 0.5s / 1s / 2s |
| 显示单位 | 自动 | 可固定为 KB/s 或 MB/s |
| 统计接口 | 全部 | 可只选 Wi-Fi 或有线 |
| 开机自启 | 关 | 写入 LaunchAgent |
| 颜色主题 | 跟随系统 | 深色/浅色/彩色（上传绿、下载蓝）|

---

## 开发环境要求

- macOS 13.0 Ventura 及以上
- Xcode 15+
- Swift 5.9+
- 无需任何第三方依赖

---

## 项目创建步骤

1. 打开 Xcode → New Project → **macOS → App**
2. 勾选 SwiftUI，取消勾选 "Include Tests"（可选）
3. 在 `Info.plist` 添加键：`Application is agent (UIElement)` = `YES`（使应用不显示在 Dock）
4. 删除默认的 `ContentView`，改为由 `StatusBarController` 驱动 UI
5. 在 Signing & Capabilities 中选择个人开发者证书（无需付费账号，本机运行即可）

---

## 关键代码片段

### App 入口

```swift
@main
struct NetworkSpeedBarApp: App {
    @NSApplicationDelegateAdaptor(AppDelegate.self) var appDelegate

    var body: some Scene {
        // 不创建任何窗口
        Settings { EmptyView() }
    }
}

class AppDelegate: NSObject, NSApplicationDelegate {
    var statusBarController: StatusBarController?

    func applicationDidFinishLaunching(_ notification: Notification) {
        statusBarController = StatusBarController()
    }
}
```

### 读取网络速率

```swift
import Darwin

struct NetworkStats {
    let rx: UInt64
    let tx: UInt64
}

func readNetworkStats() -> NetworkStats {
    var totalRx: UInt64 = 0
    var totalTx: UInt64 = 0

    var ifaddr: UnsafeMutablePointer<ifaddrs>?
    guard getifaddrs(&ifaddr) == 0, let firstAddr = ifaddr else {
        return NetworkStats(rx: 0, tx: 0)
    }
    defer { freeifaddrs(ifaddr) }

    var ptr: UnsafeMutablePointer<ifaddrs>? = firstAddr
    while let current = ptr {
        let name = String(cString: current.pointee.ifa_name)
        // 排除 loopback，只统计 AF_LINK 层数据
        if !name.hasPrefix("lo"),
           current.pointee.ifa_addr.pointee.sa_family == UInt8(AF_LINK) {
            let data = current.pointee.ifa_data
                .assumingMemoryBound(to: if_data.self).pointee
            totalRx += UInt64(data.ifi_ibytes)
            totalTx += UInt64(data.ifi_obytes)
        }
        ptr = current.pointee.ifa_next
    }
    return NetworkStats(rx: totalRx, tx: totalTx)
}
```

### 菜单栏控制器

```swift
import AppKit

class StatusBarController {
    private var statusItem: NSStatusItem
    private var timer: Timer?
    private var lastStats = readNetworkStats()

    init() {
        statusItem = NSStatusBar.system.statusItem(withLength: NSStatusItem.variableLength)
        startMonitoring()
    }

    private func startMonitoring() {
        timer = Timer.scheduledTimer(withTimeInterval: 1.0, repeats: true) { [weak self] _ in
            self?.updateSpeed()
        }
    }

    private func updateSpeed() {
        let current = readNetworkStats()
        let rxSpeed = current.rx - lastStats.rx   // bytes/s
        let txSpeed = current.tx - lastStats.tx
        lastStats = current

        let display = "\(formatSpeed(txSpeed)) ↑  \(formatSpeed(rxSpeed)) ↓"
        DispatchQueue.main.async {
            self.statusItem.button?.title = display
        }
    }

    private func formatSpeed(_ bytesPerSec: UInt64) -> String {
        switch bytesPerSec {
        case 0..<1_024:             return "\(bytesPerSec) B/s"
        case 1_024..<1_048_576:     return String(format: "%.1f KB/s", Double(bytesPerSec) / 1_024)
        case 1_048_576..<1_073_741_824: return String(format: "%.1f MB/s", Double(bytesPerSec) / 1_048_576)
        default:                    return String(format: "%.2f GB/s", Double(bytesPerSec) / 1_073_741_824)
        }
    }
}
```

---

## 性能指标目标

| 指标 | 目标值 |
|------|--------|
| CPU 占用 | < 0.5% |
| 内存占用 | < 10 MB |
| 启动时间 | < 0.5 秒 |
| 刷新延迟 | ≤ 1 秒 |

---

## 后续可扩展功能

- [ ] 历史流量图表（过去 60 秒折线图，显示在下拉 popover 中）
- [ ] 按进程显示流量（需要 Network Extension 权限）
- [ ] 流量超限提醒（月度流量阈值告警）
- [ ] 导出流量日志（CSV 格式）
- [ ] Homebrew Cask 分发

---

## 文件结构（最终）

```
NetworkSpeedBar/
├── NetworkSpeedBar.xcodeproj
├── NetworkSpeedBar/
│   ├── NetworkSpeedBarApp.swift
│   ├── AppDelegate.swift
│   ├── StatusBarController.swift
│   ├── NetworkMonitor.swift
│   ├── SpeedFormatter.swift
│   └── Info.plist
└── 技术文档.md
```
